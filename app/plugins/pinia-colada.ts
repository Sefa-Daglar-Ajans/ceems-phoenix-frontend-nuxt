export default defineNuxtPlugin(() => {
  // @pinia/colada-nuxt module auto-registers the plugin.
  // This file serves as an extension point for future query/mutation defaults
  // when the NestJS backend is integrated.
})
